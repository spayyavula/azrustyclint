apiVersion: v1
kind: Service
metadata:
  name: rustyclint-api
  namespace: rustyclint
  labels:
    app: rustyclint
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: rustyclint
    component: api
