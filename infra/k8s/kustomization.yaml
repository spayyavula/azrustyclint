apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rustyclint

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml

commonLabels:
  app.kubernetes.io/name: rustyclint
  app.kubernetes.io/managed-by: kustomize
